{
  "scripts": {
    "dokku": {
      "predeploy": "sh -c './goose -dir migrations postgres \"$DATABASE_URL\" up'"
    }
  },
  "healthchecks": {
    "web": [
      {
        "type": "startup",
        "name": "web check",
        "path": "/api/health",
        "attempts": 10,
        "wait": 5,
        "timeout": 3
      }
    ]
  }
}
