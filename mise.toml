[tools]
go = "1.24"
node = "22"

[env]
# Database
POSTGRES_DB = "retrotriage"
POSTGRES_USER = "postgres"
POSTGRES_PASSWORD = "postgres"
DATABASE_URL = "postgres://postgres:postgres@localhost:5432/retrotriage?sslmode=disable"

# Application
PORT = "8080"
ENVIRONMENT = "development"

# OpenAI
# OPENAI_API_KEY = "sk-..."
